<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <properties resource="database.properties"></properties>

    <typeAliases>
        <!--定义别名-->
        <typeAlias type="example.pojo.User" alias="_User"></typeAlias>
        <!--为包下的所有实体类设置别名 默认为类名-->
        <package name="example.pojo"></package>
    </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <!-- 配置数据库连接信息 -->
            <dataSource type="POOLED">
                <property name="driver" value="${mysql.driver}"/>
                <property name="url"
                          value="${jdbc.url}"/>
                <property name="username" value="${jdbc.name}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>


    <mappers>
        <!--注册userMapper.xml文件-->
        <mapper resource="example/mapping/userMapper.xml"></mapper>
        <!--注册UserMapper映射接口-->
        <mapper class="example.mapping.UserMapperI"></mapper>
        <!--注册orderMapper.xm文件-->
        <mapper resource="example/mapping/orderMapper.xml"></mapper>
        <!--注册classMapper.xml文件-->
        <mapper resource="example/mapping/classMapper.xml"></mapper>
    </mappers>


</configuration>